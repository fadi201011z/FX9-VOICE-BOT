// ── Bot Configuration ─────────────────────────────────────────────────
// User IDs allowed to use /setup (bot owner + developer)
// Replace with actual Discord User IDs (right-click user → Copy ID)
module.exports = {
  AUTHORIZED_SETUP_IDS: [
    "YOUR_OWNER_ID_HERE",      // ← ضع معرّفك هنا (مالك البوت)
    "DEVELOPER_ID_HERE",       // ← ضع معرّف المطور هنا
  ],
};
